import type * as RDF from '@rdfjs/types';
import { Member, RelationType } from '@treecg/types';
import { Collection } from "mongodb";
export declare type MongoFragment = {
    id?: string;
    streamId: string;
    value?: string;
    relations: {
        type: RelationType;
        value?: string;
        bucket: string;
        path?: string;
        timestampRelation?: boolean;
    }[];
    members?: string[];
    count: number;
    timeStamp?: string;
};
export declare type ExtractIndices = (member: Member, mongo: Collection<MongoFragment>, timestampPath?: RDF.Term) => Promise<void>;
export interface Fragment {
    streamId: string;
    extract: ExtractIndices;
}
export declare function handleTimestampPath(id: string, streamId: string, path: string, timestampValue: string, memberId: string, mongo: Collection<MongoFragment>, maxSize: number): Promise<void>;
