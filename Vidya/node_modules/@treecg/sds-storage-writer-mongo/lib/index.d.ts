import type * as RDF from '@rdfjs/types';
import { Stream } from "@treecg/connector-types";
export declare function ingest(data: Stream<RDF.Quad[]>, metadata: Stream<RDF.Quad[]>, metacollection: string, dataCollection: string, indexCollectionName: string, mUrl?: string, maxSize?: number): Promise<void>;
