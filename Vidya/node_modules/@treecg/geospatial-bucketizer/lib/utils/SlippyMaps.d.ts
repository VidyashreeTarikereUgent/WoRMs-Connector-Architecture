import type * as RDF from '@rdfjs/types';
export declare class SlippyMaps {
    readonly zoomLevel: number;
    constructor(zoomLevel: number);
    calculateTiles(term: RDF.Term): Map<number, number[]>;
    private toGeoJson;
    getTileBoundingBoxWktString(x: number, y: number, zoom: number): string;
    mergePolygons(polygonWktStringA: string, polygonWktStringB: string): any;
    private wktToGeoJson;
    private longitudeToTile;
    private latitudeToTile;
    private flattenArray;
}
